import { getAndRemoveItems } from '@writetome51/array-get-and-remove-items';


// index can be negative or positive.

export function removeItems(index, array): void {
	getAndRemoveItems(index, array);
}
